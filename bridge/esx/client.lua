if Framework.Initials ~= "esx" then return end

function Framework.IsPlayerWeaingGloves()
    -- implement your own logic since esx doesn't offer that by default
end

function Framework.CreateFingerPrintEvidence(coords)
    coords = coords or GetEntityCoords(cache.ped)
    -- implement your own logic since esx doesn't offer that by default
end